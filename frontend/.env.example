# VITE — API base
# Dev (proxy Vite) e produção atrás de nginx: use /api
VITE_API_URL=/api

# Só se a API estiver em outro host (sem proxy no mesmo domínio):
# VITE_API_URL=https://api.exemplo.com/api
