# VITE — desenvolvimento local
# Use /api para proxy Vite (recomendado). Evita NetworkError por CORS (localhost vs 127.0.0.1).
VITE_API_URL=/api

# Produção / preview sem proxy:
# VITE_API_URL=https://sua-api.exemplo.com/api
